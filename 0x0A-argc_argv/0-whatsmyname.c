#include <stdio.h>
/**
 * main - Entry point
 * @argc: an integar
 * @argv: a character
 * Return: Always 0 (success)
 */
int main(int argc, char const *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);
	return (0);
}
