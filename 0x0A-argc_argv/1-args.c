#include <stdio.h>
/**
 * main - Entry point
 * @argc: an integar
 * @argv: a string
 * Return: Always 0 (success)
 */
int main(int argc, char *argv[])
{
	(void) argv;
	printf("%d\n", argc);
	return (0);
}
